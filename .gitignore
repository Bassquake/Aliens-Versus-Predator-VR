platform/android/.idea/
# CMake build trees for the Linux target (build-linux.sh). The finished binaries go
# to build/linux/<arch>/, which has its own .gitignore.
.build-linux/
# Same for the macOS target (build-mac.sh) -> build/mac/<arch>/.
.build-mac/
.claude/settings.local.json
CLAUDE.md
platform/windows/enc_temp_folder/
platform/windows/RCb24708
platform/windows/RCa24708
