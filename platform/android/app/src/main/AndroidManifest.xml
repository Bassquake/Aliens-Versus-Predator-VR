<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:horizonos="http://schemas.horizonos/sdk">

    <!-- Horizon OS SDK floor. minSdkVersion=74 (OS v74+) excludes Quest 1, which is
         EOL at ~v58 — on older OS (e.g. v50) the app launches but the runtime won't
         grant immersive/focus, so you get the 3-dot loading spinner and dead
         controllers. Commented out to drop the OS-version floor for Quest 1 support.
         Restore (or set a value the target store requires) for Store submissions. -->
    <!--
    <horizonos:uses-horizonos-sdk
        horizonos:minSdkVersion="74"
        horizonos:targetSdkVersion="83" />
    -->


    <!-- The engine renders with OpenGL ES 3.0 (#version 300 es shaders), so require
         only 3.0 — requiring 3.2 needlessly excludes older GPUs/drivers (e.g. Quest 1). -->
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <!-- Vulkan is NOT used by this app (GLES-only renderer). Declared non-required so
         devices without Vulkan 1.1 / hardware level 1 (e.g. Quest 1) aren't excluded. -->
    <uses-feature android:name="android.hardware.vulkan.level" android:required="false" android:version="1" />
    <uses-feature android:name="android.hardware.vulkan.version" android:required="false" android:version="0x00401000" />

    <!-- VR Head Tracking (standard OpenXR requirement) -->
    <uses-feature android:name="android.hardware.vr.headtracking" android:required="true" android:version="1" />

    <!-- Touchscreen not required for VR -->
    <uses-feature
        android:name="android.hardware.touchscreen" android:required="false" />

    <!-- Game controller support -->
    <uses-feature
        android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature
        android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature
        android:name="android.hardware.usb.host" android:required="false" />

    <!-- Allow access to the vibrator (for controller haptics) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- LAN multiplayer: sockets need INTERNET; discovery broadcast reception
         needs a Wi-Fi multicast lock, which needs these Wi-Fi permissions. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- OpenXR permissions (for runtime broker communication) -->
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR" />
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR_SYSTEM" />

    <uses-feature android:name="oculus.software.handtracking" android:required="false" />
    <uses-feature android:name="com.oculus.feature.PASSTHROUGH" android:required="false" />
    <uses-feature android:name="com.oculus.feature.OPENXR" android:required="true" />

    <!-- OpenXR runtime/layer queries -->
    <queries>
        <provider android:authorities="org.khronos.openxr.runtime_broker;org.khronos.openxr.system_runtime_broker" />
        <intent>
            <action android:name="org.khronos.openxr.OpenXRRuntimeService" />
        </intent>
        <intent>
            <action android:name="org.khronos.openxr.OpenXRApiLayerService" />
        </intent>
    </queries>

    <application
        android:name=".InstallAssets"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:hardwareAccelerated="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <uses-native-library android:name="libopenxr.google.so" android:required="false" />
        <!-- com.oculus.supportedDevices is DELIBERATELY ABSENT. Do not put it back, in any
             form - it is worse than useless here, in two ways that pull against each other:

               - Present but not listing the headset you are on RESTRICTS it. With
                 "quest|quest2|questpro" a Quest 3 offered only 72/80/90 Hz; the 120 Hz
                 option did not exist.
               - Adding "quest3" fixed that and then HARD-ABORTED Quest 1: its v50-era
                 runtime parses this list and SIGABRTs on any token it does not know
                 ("Unknown device to support: quest3"), and quest3/quest3s postdate it.

             Removing the key entirely satisfies both - a Quest 3 keeps 120 Hz and a Quest 1
             runs - because absence means no restriction rather than nothing supported, and
             sideloaded apps are not gated by this list at all. One APK serves every headset.
             Verified on both devices 2026-09-03. Trimming the list back to old-only tokens
             would also stop the abort, but re-caps the Quest 3 to 90 Hz, so it is not a
             substitute for deleting it. -->
        <meta-data android:name="com.oculus.openxr.application" android:value="true" />
        <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
        <!--<meta-data android:name="com.oculus.intent.category.2D" android:value="true" />-->
        <meta-data android:name="com.oculus.handtracking.version" android:value="V2.0" />
        <meta-data android:name="com.oculus.handtracking.frequency" android:value="HIGH" />
        <!-- Flat / Panel Mode Entry -->
        <activity
            android:name=".MainActivity"
            android:configChanges="density|keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|uiMode"
            android:exported="true"
            android:alwaysRetainTaskState="true"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:excludeFromRecents="false"
            tools:ignore="NonResizeableActivity"
            android:resizeableActivity="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.oculus.intent.category.VR" />
                <category android:name="org.khronos.openxr.intent.category.IMMERSIVE_HMD" />
            </intent-filter>
        </activity>
        <meta-data android:name="pvr.app.type" android:value="vr" />
    </application>
</manifest>